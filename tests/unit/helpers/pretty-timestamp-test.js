
import { prettyTimestamp } from 'message-board-ip/helpers/pretty-timestamp';
import { module, test } from 'qunit';

module('Unit | Helper | pretty timestamp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = prettyTimestamp([42]);
  assert.ok(result);
});

